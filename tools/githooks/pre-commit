#!/usr/bin/env bash
# Versioned pre-commit hook (enable once per clone):
#   git config core.hooksPath tools/githooks
# Blocks commits when the classic and modular builds have drifted apart.
exec "$(git rev-parse --show-toplevel)/tools/check-drift.sh"
